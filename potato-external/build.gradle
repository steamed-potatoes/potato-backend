bootJar {
    enabled = false
}
jar {
    enabled = true
}

dependencies {
    implementation project(':potato-common')

    implementation 'org.springframework.boot:spring-boot-starter-webflux'
    api group: 'org.springframework.cloud', name: 'spring-cloud-starter-aws', version: '2.2.6.RELEASE'
}
