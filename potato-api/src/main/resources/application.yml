server:
  port: 8080
spring:
  session:
    store-type: jdbc
  profiles:
    active: local
    include:
      - domain
      - thirdparty
      - swagger
  datasource:
    data: classpath:schema/schema-session.sql
