apiVersion: v1
kind: Service
metadata:
  name: potato-api
  labels:
    app: potato-api
    env: dev
spec:
  type: ClusterIP
  selector:
    app: potato-api
    env: dev
  ports:
    - port: 8080
      targetPort: 8080
