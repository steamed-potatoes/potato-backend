apiVersion: v1
kind: Service
metadata:
  name: potato-api-test
  labels:
    app: potato-api-test
    env: test
spec:
  type: ClusterIP
  selector:
    app: potato-api-test
    env: test
  ports:
    - port: 9000
      targetPort: 8080
